{
  "name": "@softcodefr/vue-lens-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter playground dev",
    "build": "pnpm --filter './packages/*' build",
    "typecheck": "pnpm --filter './packages/*' typecheck"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
